import React from 'react';
import { Burger, RightNav } from '../'

const Header = () => {
    return (
        <nav role="navigation">
            <Burger />
        </nav>
    );
}

export default Header;
