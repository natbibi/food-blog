import React from 'react';
import { Burger } from '../'

const Header = () => {
    return (
        <nav role="navigation">
            <h1>Ste's Food</h1>
            <Burger />
        </nav>
    );
}

export default Header;
